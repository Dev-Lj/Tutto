package ch.uzh.dice;
/*
import org.mockito.Mockito;
import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.ArgumentMatchers.anyInt;
import static org.mockito.ArgumentMatchers.isNotNull;
import java.util.Random;
import org.junit.jupiter.api.Test;
*/

public class DiceManagerTest {
    /*@Test
    public void testGetRandomNumber() {
        DiceManager DM = new DiceManager();
        DM.random = Mockito.mock(Random.class);
        Mockito.when(rg.random.nextInt(anyInt())).thenReturn(5);
        DM.rollDice();
        assertEquals(5, DM.getScore());
        assertEquals(true, DM.isTutto());
        assertEquals(false, DM.hadNullTurn());
    }
    */
    //moc object diceManager
    /*
    DiceManager DM = Mockito.mock(DiceManager.class);
     */

}
